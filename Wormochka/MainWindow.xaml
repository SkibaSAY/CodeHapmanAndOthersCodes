<Window x:Class="Wormochka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wormochka"
        mc:Ignorable="d"
        Title="MainWindow" Height="733.978" Width="1219.318">
    <Grid>
        <DockPanel x:Name="Dock" Background="#FFCF6565" VerticalAlignment="Stretch" Margin="0,0,0,0">
            <Menu DockPanel.Dock="Top" Height="35">
                <Menu.Background>
                    <SolidColorBrush Color="#FF042D56"/>
                </Menu.Background>
                <Button x:Name="RunHaphman" Content="Запустить" Height="35" AutomationProperties.Name="run" Padding="0,0,0,0" BorderThickness="0,0,0,0" Width="150" Background="{x:Null}" Foreground="#FF67F3CA"/>
            </Menu>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="66*"/>
                    <ColumnDefinition Width="10*"/>
                    <ColumnDefinition Width="228*"/>
                    <ColumnDefinition Width="305*"/>
                    <ColumnDefinition Width="305*"/>
                    <ColumnDefinition Width="305*"/>
                </Grid.ColumnDefinitions>
                <ScrollViewer x:Name="inputView" Grid.Column="0" Margin="0,0,305,0" Grid.ColumnSpan="4" Foreground="#FF67F3CA">
                    <ScrollViewer.Background>
                        <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.HighlightColorKey}}"/>
                    </ScrollViewer.Background>
                </ScrollViewer>
                <ScrollViewer x:Name="codesView" Grid.Column="3" Margin="0,0,304,0" Grid.ColumnSpan="2" Foreground="#FF0078D7">
                    <ScrollViewer.Background>
                        <SolidColorBrush Color="#FF67F3CA"/>
                    </ScrollViewer.Background>
                </ScrollViewer>
                <ScrollViewer x:Name="encryptView" Grid.Column="4" Foreground="#FF67F3CA">
                    <ScrollViewer.Background>
                        <SolidColorBrush Color="#FF0078D7"/>
                    </ScrollViewer.Background>
                </ScrollViewer>
                <ScrollViewer x:Name="decryptView" Grid.Column="4" Background="#FF67F3CA" Grid.ColumnSpan="2" Margin="304,0,0,0" Foreground="#FF0078D7"/>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
